import React from 'react';
export default class ErrorBoundary extends React.Component{constructor(props){super(props);this.state={error:null}}static getDerivedStateFromError(error){return{error}}componentDidCatch(error,info){if(import.meta.env.DEV)console.error('Kleenest UI error',error,info)}handleRetry=()=>this.setState({error:null});render(){if(!this.state.error)return this.props.children;return <main className="page"><div className="empty-state"><h1>Something went wrong</h1><p>That part of Kleenest could not load correctly.</p><button className="primary" onClick={this.handleRetry}>Try again</button><button className="secondary" onClick={()=>window.location.reload()}>Reload app</button></div></main>}}
