import {useEffect,useState} from 'react';
import {ArrowLeft,Medal,RefreshCw,Trophy} from 'lucide-react';
import {Link} from 'react-router-dom';
import {getUserLeaderboard} from '../services/rewards';
import {useAuth} from '../context/AuthContext';
export default function LeaderboardPage(){const{authenticated,loading:authLoading}=useAuth();const[rows,setRows]=useState([]),[loading,setLoading]=useState(true),[error,setError]=useState('');async function load(){setLoading(true);setError('');try{setRows(await getUserLeaderboard())}catch(e){setError(e.message||'Unable to load leaderboard.')}finally{setLoading(false)}}useEffect(()=>{if(authenticated)load();else if(!authLoading)setLoading(false)},[authenticated,authLoading]);if(authLoading)return <section className="page"><div className="empty-state">Checking your session…</div></section>;if(!authenticated)return <section className="page"><div className="empty-state"><h2>Sign in to view the leaderboard</h2><Link className="primary" to="/profile">Sign in</Link></div></section>;return <section className="page"><Link className="back-link" to="/rewards"><ArrowLeft size={16}/>Rewards</Link><div className="page-header"><div><span className="eyebrow">COMMUNITY LEADERBOARD</span><h1>Top contributors</h1><p>Verified activity earns points and helps strengthen Kleenest location intelligence.</p></div><button className="secondary" onClick={load}><RefreshCw size={16}/>Refresh</button></div>{error?<div className="empty-state"><h2>Leaderboard unavailable</h2><p>{error}</p><button className="secondary" onClick={load}>Retry</button></div>:loading?<div className="empty-state">Loading leaderboard…</div>:<section className="detail-panel"><div className="panel-heading"><div><span className="eyebrow">RANKINGS</span><h2>Community contributors</h2></div><Trophy size={24}/></div>{rows.length?rows.slice(0,50).map((r,i)=><div className="reward-row" key={r.user_id||r.id||i}><div><strong><Medal size={16}/> #{i+1} {r.display_name||r.name||'Kleenest member'}</strong><span>{r.level!=null?`Level ${r.level} · `:''}{r.total_check_ins??r.check_ins??0} check-ins</span></div><b>{r.points??r.total_points??0}</b></div>):<p>No contributor data yet.</p>}</section>}</section>}
